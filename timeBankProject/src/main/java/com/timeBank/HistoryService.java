/*
 * package com.timeBank;
 * 
 * import java.util.List; import java.util.Optional; import
 * org.springframework.beans.factory.annotation.Autowired; import
 * org.springframework.stereotype.Service;
 * 
 *//**
	 * @author Link0
	 *
	 */
/*
*//**
	 * @author Link0
	 *
	 *//*
		 * @Service public class HistoryService {
		 * 
		 * @Autowired HistoryRepository repository;
		 * 
		 * public Iterable<History> findAll() { return repository.findAll(); }
		 * 
		 * public void createHistory(History history) { repository.save(history); }
		 * 
		 * public void deleteHistory(int id) { Optional<History> history =
		 * repository.findById(id); repository.delete(history.get()); }
		 * 
		 * public List<History> findByAlphaUser(User user) { List<History> history =
		 * (List<History>)repository.findByAlphaUser(user); return history; }
		 * 
		 * public List<History> findByBetaUser(User user) { List<History> history =
		 * (List<History>)repository.findbyBetaUser(user); return history; }
		 * 
		 * }
		 */
